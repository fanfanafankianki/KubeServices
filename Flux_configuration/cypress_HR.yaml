apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: cypress-custom
  namespace: staging
spec:
  interval: 1m
  chart:
    spec:
      chart: ./HelmCharts/cypress_prod
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
