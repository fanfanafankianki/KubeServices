apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "django_prod.fullname" . }}-test-connection"
  labels:
    {{- include "django_prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "django_prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
